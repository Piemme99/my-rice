# Catppuccin Backgrounds

Place your preferred Catppuccin-friendly wallpapers here.
The bootstrap script links `~/.config/myrice/current/background` to `backgrounds/default.jpg` if present, otherwise Hyprland falls back to a solid color.
